# Spec 审查 — P0/P1/P2 分类表

## 1. 审查场景说明

本文件定义 **Spec 规格文档审查** 专用分类体系。审查对象为 `.harness/temp/{vId}/spec.md`（规格文档），审查产物为 `.harness/temp/{vId}/spec-suggest.md`（校验建议文件）。

本分类直接沿用通用文档审查框架（common_review.md）的结构，在 Spec 场景下做具体化适配。

## 2. P0/P1/P2 分类表

| 等级 | 类别 | 描述 |
|------|------|------|
| 🔴 P0 | 内容遗漏 | 关键的功能规格、边界条件、异常流程缺失，导致开发无法正常推进 |
| 🔴 P0 | 逻辑矛盾 | 同一 spec 中存在相互冲突的功能描述、技术选型或数据定义 |
| 🔴 P0 | 方案缺陷 | 技术方案明显不可行、选型错误，会导致后续工作走错方向 |
| 🔴 P0 | 不可验证 | 验收标准缺失或模糊，开发完成后无法判断是否达标 |
| 🟡 P1 | 描述不清 | 表述含糊、有歧义，多个读者可能产生不同理解 |
| 🟡 P1 | 边界缺失 | 输入校验、空状态、并发、超时等边界条件未覆盖 |
| 🟡 P1 | 上下文断裂 | 依赖外部系统或前置假设但不做说明，读者无法理解上下文 |
| 🟡 P1 | 不完整 | 某部分有改进空间，但当前版本尚可工作 |
| 🟢 P2 | 措辞润色 | 表述可更精炼或清晰，但不影响理解 |
| 🟢 P2 | 格式风格 | 排版、标记风格不一致 |
| 🟢 P2 | 过度设计 | 为不确定的未来做的提前扩展 |

> 分类表直接沿用通用文档框架（common_review.md），与文档审查标准保持一致。

## 3. 检查重点（Reviewer Agent 注入用）

Reviewer Agent 启动时，将以下检查重点注入 prompt，指导审查方向：

- 功能规格是否完整覆盖用户需求
- 技术方案是否可行且合理（选型理由、替代方案对比）
- 边界条件和异常场景是否覆盖（输入校验、空值、网络超时、并发冲突）
- 非功能需求是否明确（性能指标、安全要求、兼容性）
- 验收标准是否可验证（不应出现"系统应流畅运行"这类模糊表述）
- 涉及的文件结构是否完整列出

## 4. 判断标准

父 Agent 判断 spec 可以退出循环的标准：

- **P0 列表为空** — 无阻塞问题（无遗漏、无矛盾、方案可行、验收标准可衡量）
- **P1 列表中无"内容缺失"类的实质性问题** — 仅含措辞或格式建议，可退出循环
