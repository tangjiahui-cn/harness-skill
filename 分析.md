# `spec-create-harness` Skill 处理流程分析

> 分析对象：`skills/spec-create-harness/SKILL.md`（当前工作流的事实来源），
> 佐证：`references/review-spec.md` / `review-plan.md` / `review-exec.md`、`.claude/spec/create_harness.md`、`.claude/spec/review.md`（框架层设计文档）。

---

## 1. 总体架构：父 Agent 编排 + 5 种子 Agent

整个 skill 把一次需求从"模糊文本"推进到"已提交、已验证、可推送的代码"，核心是**一套父子 Agent 协作模型**：

```
┌─────────────────────────────────────────────────────────────────┐
│                          父 Agent（主对话 Agent）                  │
│                                                                 │
│   职责：与用户交互 │ 启动子Agent │ 读报告做决策 │ 管理状态文件         │
│         │ 管理临时建议文件 │ 分支安全 │ 归档 │ 推送                  │
└───────┬────────────────────────────┬───────────────────────────┘
        │ 启动 + 注入标准/上下文      │ 报告（文本） + 产物文件
        ▼                            ▼
┌──────────────────────┐    ┌──────────────────────┐
│  spec-generator      │    │  spec-reviewer        │
│  生成/修补 spec.md   │    │  校验 spec → spec-suggest.md  │
├──────────────────────┤    ├──────────────────────┤
│  plan-generator      │    │  plan-reviewer        │
│  生成/修补 plan.md   │    │  校验 plan → plan-suggest.md  │
├──────────────────────┼────┼──────────────────────┤
│  execution           │    │  （无对应 reviewer    │
│  执行 plan + 提交     │    │    —— 见 §7 观察②）   │
└──────────────────────┘    └──────────────────────┘

通信介质：产物文件（spec.md / plan.md）+ 建议文件（*-suggest.md）+ 状态文件（state.json）
关键规则：子 Agent 之间不直接交互，所有消息经父 Agent 中转（文件 + 文本报告）。
```

**三层决策模型**（谁对"产出"有最终话语权）：

| 层级 | 角色 | 决策内容 |
|------|------|---------|
| 内容层 | 子 Agent（generator / reviewer / execution） | 生成什么内容、怎么改 |
| 质量层 | 父 Agent | 读 reviewer 的 P0/P1/P2 输出，判断"是否有实质性问题"、是否继续循环 |
| 终局层 | 用户 | 是否确认、是否验收、是否推送 |

> 设计意图：父 Agent 是唯一"有状态 + 有决策权 + 面向用户"的节点；子 Agent 全部**无状态**（每次循环新实例），只通过文件交接上下文。这保证了产物质量判断不交给"提出建议的人"（reviewer 只提建议、不改文件），也保证了父 Agent 不会过早陷进内容细节而丢失全局。

---

## 2. 角色清单与职责边界

### 2.1 父 Agent（主对话）

| 职责 | 说明 | 对应 SKILL.md 章节 |
|------|------|------------------|
| 需求澄清 | 复述、追问模糊点、确认范围 | Step 1 |
| 初始化 | 建目录、生成 `name/shortId/vId`、写 `state.json` | Step 2 |
| 启动子 Agent | 携带上下文/审查标准注入 prompt 启动各子 Agent | §3.1 / §4.1 / §5.1 |
| 面向用户 | 展示产物概要、给出 1-5 编号选项、收集选择 | §3.2 / §4.2 / §5.2 |
| 质量决策 | 读 `*-suggest.md`，对照判断标准决定"进修补 or 退出循环" | §3.3 / §4.3 |
| 驱动循环 | 交替启动 reviewer ↔ generator（最多 5 轮） | §3.3 / §4.3 |
| 文件管理 | 创建/删除 `spec-suggest.md` / `plan-suggest.md`（生命周期由父 Agent 独占） | §3.3 步骤 5/9 |
| 状态管理 | 维护 `state.json` 的 `status/step/currentStep/waitingFor/updatedAt` | 状态表 |
| 分支安全 | 保护分支上询问是否建功能分支 | §5.1 |
| 纠错回退 | 执行出错时 `git reset --soft` / `git restore`，重定向重新执行 | §5.2 情况 B |
| 收尾 | 验收、归档到 history、询问推送 | §5.2 / Step 6 |
| 中断恢复 | 启动时扫描 `.harness/temp/`，按 `status + waitingFor` 跳转到断点 | 中断恢复机制 |

**父 Agent 的"红线"**：不亲自写 spec/plan/代码，不直接改产物——这些全交给子 Agent。父 Agent 只做编排、判断与交互。

### 2.2 子 Agent 一览

| 子 Agent | 初始模式职责 | 修补模式职责 | 输出 | 不允许做的事 |
|----------|-------------|-------------|------|------------|
| `spec-generator` | 读项目上下文+需求 → 生成 `spec.md`（背景目标/功能/非功能/技术方案/文件结构） | 读 `spec-suggest.md`，逐条采纳修改 `spec.md` | `spec.md` | 不质疑 reviewer；不透支（不擅自做建议以外的优化） |
| `spec-reviewer` | 读 `spec.md` → 用 review-spec.md 的 P0/P1/P2 标准审查 → 写 `spec-suggest.md` | —（无修补职责） | `spec-suggest.md` | 不直接修改 `spec.md` |
| `plan-generator` | 读已确认的 `spec.md` → 拆解为可独立提交的步骤 + 汇总 `allowedPaths` | 读 `plan-suggest.md` 逐条修补 | `plan.md` | 同 spec-generator |
| `plan-reviewer` | 读 `plan.md` → 用 review-plan.md 标准审查 → 写 `plan-suggest.md` | — | `plan-suggest.md` | 不直接修改 `plan.md` |
| `execution` | 按 `plan.md` 逐步骤执行：声明文件 → 校验 allowedPaths → 实现 → 分层验证 → 测试 → commit → 更新 currentStep | —（按 plan 执行，不回改 plan） | 提交记录 + 报告 | 不改 `plan.md`；越界文件一律回退/上报 |

### 2.3 边界设计的三个核心对称

1. **生成 vs 校验 对称**：spec/plan 两阶段都是"generator 产出 → reviewer 审查 → 父 Agent 决断"的同一模式，只是审查标准文件不同（review-spec.md / review-plan.md）。
2. **"提建议者不改文件"**：reviewer 只写 `*-suggest.md`，动手改的是 generator；父 Agent 负责把建议"翻译"成对 generator 的启动指令。这防止 reviewer 自审自改失去独立性。
3. **"执行者不改计划"**：execution 是 plan 的忠实执行者，遇到问题向上报告而非就地改 plan；改 plan/spec 的权力保留在用户 + 父 Agent。

---

## 3. 通信机制：文件即消息

子 Agent 之间**不直接交互**（`§交互要点`、review.md §7.2），所有信息经父 Agent 中转，介质分两类：

| 介质 | 谁写 | 谁读 | 用途 |
|------|------|------|------|
| `spec.md` | spec-generator | 父 Agent / spec-reviewer / plan-generator | 需求规格（事实来源） |
| `spec-suggest.md` | spec-reviewer | 父 Agent / spec-generator（修补） | 校验建议（临时消息） |
| `plan.md` | plan-generator | 父 Agent / plan-reviewer / execution | 执行计划 |
| `plan-suggest.md` | plan-reviewer | 父 Agent / plan-generator（修补） | 校验建议（临时消息） |
| `state.json` | 父 Agent（独占写入） | 父 Agent / execution | 工作流状态机 |
| 文本报告 | 各子 Agent | 父 Agent | 完成/出错/暂停的通知与概要 |

**为什么用文件而不是子 Agent 直接对话**：
- 每次启动都是**新实例（无记忆）**，上下文只能靠文件 + 启动 prompt 传递 → 文件是唯一的"跨实例记忆"；
- 父 Agent 通过"读不读 / 删不删建议文件"就能**暂停或终止循环**，文件生命周期由父 Agent 独占管理（review.md §5.3），天然支持断点恢复；
- 建议文件在每轮循环结束时由父 Agent **删除**，避免下一轮 reviewer 被旧建议污染（视角刷新）。

---

## 4. 调用过程（分阶段时序）

### 阶段 A：Spec 生成（Step 1→3）

```
用户 ──文本需求──▶ 父Agent ──澄清/确认──▶ 用户
父Agent ──初始化──▶ state.json(status=initialized)
父Agent ──启动──▶ spec-generator(初始) ──写──▶ spec.md
                            │──报告──▶ 父Agent
父Agent ──展示概要+选项──▶ 用户
   ├─[2]直接确认 ──▶ status=spec_confirming ──▶ 阶段B
   ├─[3]重新生成 ──▶ 删spec.md ──▶ 重启 spec-generator
   ├─[4]补充信息 ──▶ waitingFor=user_supplement ──▶ 重启 spec-generator(带补充)
   ├─[5]手动修改 ──▶ 用户编辑 spec.md ──▶ 回到选项
   └─[1]AI校验 ──▶ status=spec_reviewing ──▶ 进入校验循环（见 §5）
```

### 阶段 B：Plan 生成（Step 4）

结构与阶段 A **完全同构**，只是：
- 输入换成"已确认的 `spec.md`"；
- generator 额外汇总 `allowedPaths`（`state.json.plan.allowedPaths` 在确认时写入，§4.5）；
- 补充信息若涉及需求变更，父 Agent 先引导**回 Step 3 改 spec**，再基于新 spec 重新生成 plan。

### 阶段 C：执行（Step 5）

```
父Agent ──分支安全检查──▶ 保护分支？──是──▶ 询问用户是否建 feat/{vId}
父Agent ──启动──▶ execution（注入 plan.md + state.json + 项目上下文）
execution 每步循环：
  声明文件 → 校验 allowedPaths（git status 干净性 + 逐文件校验）
  实现 → 实现后校验（越界则 git restore）→ 编译/类型检查 → lint → 测试
  commit（Conventional Commits）→ 更新 currentStep → 报告父Agent
execution ──全部完成──▶ 报告 ──▶ 父Agent 展示验收界面 ──▶ 用户
   ├─[1]验收通过 ──▶ 归档 temp→history ──▶ 询问推送（Step 6）
   └─[2]补充信息 ──▶ git log 定位 → git reset --soft 回退 → 从指定步重启 execution
execution ──出错/越界/测试失败──▶ 暂停报告 ──▶ 父Agent 展示问题界面 ──▶ 用户
   ├─[1]重执行该步 ──▶ 回退提交/清理工作区 → 重启 execution(从 Step N)
   ├─[2]全部重来 ──▶ git reset --soft 回退所有提交 → 从 Step 1 重启
   └─[3]补充信息 ──▶ 更新 spec/plan/allowedPaths → 从当前步继续
```

---

## 5. AI 校验循环（核心编排逻辑，spec/plan 通用）

```
循环（最多 5 轮）：
 ① 父Agent ──启动新实例──▶ reviewer（prompt 注入 references/review-*.md 的
                            P0/P1/P2 分类表 + 检查重点 + 判断标准）
 ② reviewer ──读──▶ 产物文件（spec.md / plan.md）
 ③ reviewer ──写──▶ {spec,plan}-suggest.md ──报告──▶ 父Agent
 ④ 父Agent ──读──▶ suggest 文件 ──对照判断标准──▶
     ├─ P0 为空 且 P1 无"内容缺失/步骤遗漏/依赖错误"类实质问题
     │    ──▶ 删 suggest 文件 ──▶ 退出循环 ✅
     └─ 存在实质问题 ──▶ 启动 generator(修补模式)，传递 suggest 内容
 ⑤ generator(修补) ──读──▶ suggest 文件 ──逐条采纳──▶ 修改产物文件 ──报告──▶ 父Agent
 ⑥ 父Agent ──删除──▶ suggest 文件（防旧建议污染下一轮）──▶ 回到 ①
```

**关键参数与设计点**：
- 循环上限 **5 轮**，超限强制退出，以当前版本为准；
- reviewer **每次新实例 + 以"第一次审查"心态**重新审视，不因上轮提过建议就放行（避免自满/路径依赖）；
- generator 修补模式 **默认采纳、不质疑、不透支**（review.md §7.1），防止修补过程引入新争议；
- 判断"是否实质性问题"的**权力在父 Agent**，不在 reviewer——reviewer 只产出分级建议，父 Agent 拿着判断标准做取舍（"建议进入修补"与"可人工跳过"之间存在父 Agent 的裁量空间）。

---

## 6. 状态机：父 Agent 控制进度的中枢

`state.json` 是父 Agent 编排的落点，子 Agent（除 execution 更新 `currentStep` 外）**不写状态文件**：

```
initialized → spec_pending → [spec_reviewing] → spec_confirming
           → plan_pending → [plan_reviewing] → plan_confirming
           → executing → acceptance → accepted → completed
```

- `[spec_reviewing]` / `[plan_reviewing]` 仅当用户选"AI 校验"时进入；
- `step`（大阶段）vs `currentStep`（plan 内步骤序号，仅 step=5 使用）严格区分；
- `waitingFor: "user_supplement"` 标记"等用户补充信息"，与 `status` 独立；
- **中断恢复**完全由状态文件驱动：父 Agent 启动时扫描 `.harness/temp/`，按 `status + waitingFor` 精确跳回断点（如 `spec_reviewing` 时会检测 suggest 文件是否存在，判断是回到循环还是回到选择界面）。

---

## 7. 职责边界的设计观察

### 优点
1. **职责单向**：父 Agent 不写产物、子 Agent 不面对用户、reviewer 不改文件、execution 不改计划——每个角色只做一类事，交接点清晰。
2. **决策权集中**：质量判断（是否合格）唯一握在父 Agent 手，配合"每轮删建议文件"实现了可终止、可恢复的循环。
3. **无状态子 Agent 降低偏见**：新实例 + 视角刷新，reviewer 不会因历史上下文产生路径依赖。
4. **文件即协议**：产物/建议/状态三类文件的读写方、生命周期都有明确归属，天然支持断点续跑。

### 观察与潜在不一致（供参考）
1. **文档漂移**：`SKILL.md` 已在最近提交（`1adf9bf feat: 生成后用户选择是否 AI 校验`）中更新为"生成后先让用户选是否校验"，但 `.claude/spec/create_harness.md` 仍是旧的"生成后直接进校验循环"结构。当前以 `SKILL.md` 为准，spec 文档待同步。
2. **exec 审查未接入执行阶段**：`references/review-exec.md`（代码导向 P0/P1/P2 分类，对应产物 `exec-suggest.md`）已随 `d490946` 落地，但 `SKILL.md` 的 Step 5 执行流程中**没有**对应的 `exec-reviewer` 子 Agent / AI 校验循环——执行质量目前只靠 execution 自带的"分层验证 + 测试"保证，缺少 spec/plan 那样的独立审查回路。属于"分类表已备、流程未接"的预留状态。
3. **父 Agent 的判断依赖"读取 reviewer 报告"**：§3.3/§4.3 要求父 Agent 读 `*-suggest.md` 后判断实质性问题，但 SKILL.md 未给"如何解析/是否要求结构化精简版"做强制约定（review.md §5.2 提供了精简版格式）；实际执行时依赖父 Agent 对 P0/P1 类别的理解，容错取决于报告规范性。
4. **allowedPaths 的维护**：手动修改 plan（§4.2 选项 5）时要求用户"若改文件需同步更新 allowedPaths"，这是流程中少数需要用户配合维护状态文件的地方，存在漏更导致执行期越界误报的隐患。

---

## 8. 一句话总结

> **父 Agent 是"编排中枢 + 用户接口 + 质量裁判"，五个子 Agent 是"无状态的产出单元"（generator 生产、reviewer 审查、execution 执行），彼此只通过文件间接通信、经父 Agent 中转**；`state.json` 是全程状态的唯一落点，`*-suggest.md` 是校验循环的消息载体且生命周期由父 Agent 独占，最终形成"生成 → 校验 → 决断 → 修补"可终止、可恢复的闭环。
